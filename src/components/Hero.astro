---
---

<section class="hero" id="top">
  <div class="signature" aria-hidden="true"></div>

  <div class="meta reveal d1">
    <span>↓ ALVIN REYES</span>
    <span>FOUNDER &mdash; <b>AR DATA</b></span>
    <span>20 YEARS / NO VC / NO BENCH</span>
  </div>

  <h1 class="reveal d2">
    We don&rsquo;t prototype<br/>
    and disappear.<br/>
    We <span class="ital">ship.</span>
  </h1>

  <p class="sub reveal d3">
    Twenty years at <strong>Oracle, IBM, HP, Macquarie, Scotiabank, and Protocol Labs</strong>
    taught me what production looks like. I built AR Data to deliver the
    AI &amp; blockchain systems most consultancies fake &mdash; bootstrapped,
    no investors, no prototype-and-disappear. <strong>The AR is me.</strong>
  </p>

  <div class="cta reveal d4">
    <a href="https://calendly.com/alvin-ardata/discovery-ai-partnership" target="_blank" rel="noopener" class="btn">Book a discovery call <span class="arr">→</span></a>
    <a href="#workflow" class="btn ghost">See the workflow <span class="arr">↓</span></a>
  </div>
</section>

<style>
  .hero {
    padding-top: clamp(96px, 14vw, 184px);
    padding-bottom: clamp(80px, 12vw, 160px);
    position: relative;
    overflow: hidden;
  }

  .hero .meta {
    font-family: var(--mono);
    font-size: 11px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--muted);
    display: flex;
    gap: 24px;
    margin-bottom: 56px;
    flex-wrap: wrap;
  }

  .hero .meta b {
    color: var(--ink);
    font-weight: 500;
  }

  .hero h1 {
    font-family: var(--serif);
    font-size: clamp(56px, 11vw, 168px);
    line-height: 0.9;
    letter-spacing: -0.025em;
    margin: 0;
    text-wrap: balance;
    position: relative;
  }

  .hero .sub {
    margin-top: 48px;
    max-width: 720px;
    font-size: clamp(18px, 1.6vw, 22px);
    line-height: 1.5;
    color: var(--ink-soft);
    text-wrap: pretty;
  }

  .hero .sub strong {
    color: var(--ink);
    font-weight: 500;
  }

  .hero .signature {
    position: absolute;
    right: calc(var(--pad-x) * -0.6);
    top: 110px;
    width: clamp(180px, 24vw, 360px);
    height: clamp(180px, 24vw, 360px);
    background: var(--accent);
    border-radius: 50%;
    z-index: 0;
    opacity: 0.95;
    mix-blend-mode: normal;
    animation: drift 14s ease-in-out infinite alternate;
  }

  .hero .signature::after {
    content: "AR";
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var(--serif);
    font-style: italic;
    color: var(--bg);
    font-size: clamp(80px, 12vw, 180px);
    letter-spacing: -0.04em;
  }

  .hero > * {
    position: relative;
    z-index: 1;
  }

  .hero .cta {
    margin-top: 64px;
    display: flex;
    gap: 14px;
    flex-wrap: wrap;
    font-family: var(--mono);
    font-size: 12px;
  }
</style>
